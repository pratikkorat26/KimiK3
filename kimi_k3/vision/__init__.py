"""
kimi_k3/vision/ — native vision (deferred).

Kimi K3 ships MoonViT-V2 (≈401M params) as a native vision encoder and
is multimodal (text + image) in one model. Vision wiring is intentionally
out of scope for this architecture pass; this package exists so the slot
is visible in the tree.
"""
